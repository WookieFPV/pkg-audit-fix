{
  "name": "pkg-audit-fix",
  "version": "0.1.0",
  "description": "Audit and remediate dependency vulnerabilities across pnpm, npm, and bun projects.",
  "type": "module",
  "bin": {
    "pkg-audit-fix": "./dist/cli.mjs"
  },
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "vitest run",
    "test:watch": "vitest",
    "package:check": "publint && npm pack --dry-run",
    "ci": "biome check . && tsc --noEmit && vitest run && tsdown && publint"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "import": "./dist/index.mjs"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "sideEffects": false,
  "engines": {
    "node": ">=20.19"
  },
  "keywords": [
    "audit",
    "security",
    "dependencies",
    "pnpm",
    "npm",
    "bun",
    "cli"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/example/pkg-audit-fix.git"
  },
  "dependencies": {
    "package-manager-detector": "1.6.0"
  },
  "devDependencies": {
    "@arethetypeswrong/core": "0.18.2",
    "@biomejs/biome": "2.4.10",
    "@types/node": "25.5.0",
    "publint": "0.3.18",
    "tsdown": "0.21.7",
    "typescript": "6.0.2",
    "vitest": "4.1.2"
  }
}
